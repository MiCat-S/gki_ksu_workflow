name: Module - Build Kernel Image

on:
  workflow_call:
    inputs:
      kernel_version:
        required: true
        type: string
      sub_level:
        required: true
        type: string
      variant:
        required: true
        type: string
      artifact_suffix:
        required: false
        type: string
        default: ''
      use_droidspaces:
        required: true
        type: boolean
      use_rekernel:
        required: false
        type: boolean
        default: true
      use_susfs:
        required: true
        type: boolean
      susfs_repo:
        required: false
        type: string
        default: 'GitLab'
      kowsu_branch:
        required: false
        type: string
        default: 'KOWX712/KernelSU:master'
      kernelsu_next_branch:
        required: false
        type: string
        default: 'KernelSU-Next/KernelSU-Next:dev'
      kernelsu_official_branch:
        required: false
        type: string
        default: 'tiann/KernelSU:main'
      resukisu_branch:
        required: false
        type: string
        default: 'ReSukiSU/ReSukiSU:main'
      kernelsu_xx_branch:
        required: false
        type: string
        default: 'backslashxx/KernelSU:master'
      hook_mode:
        required: false
        type: string
      ksu_debug:
        required: false
        type: boolean
        default: false
      upload_vmlinux:
        required: false
        type: boolean
        default: false
      source_mirror:
        required: true
        type: string
      use_ccache:
        required: false
        type: boolean
        default: true
      use_adios:
        required: false
        type: boolean
        default: false
      use_lz4_zstd:
        required: false
        type: boolean
        default: false
      susfs_commit:
        required: false
        type: string
        default: ''
      kernel_localversion:
        required: true
        type: string
      build_epoch:
        required: true
        type: string
      build_timestamp:
        required: true
        type: string
      build_user:
        required: true
        type: string
      build_host:
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 300
    env:
      DS_SUFFIX: ${{ inputs.use_droidspaces && '-DS' || '' }}
      SUSFS_SUFFIX: ${{ inputs.use_susfs && '-SUSFS' || '' }}
      ADIOS_SUFFIX: ${{ inputs.use_adios && inputs.kernel_version != '6.1' && '-ADIOS' || '' }}
      ZRAM_SUFFIX: ${{ inputs.use_lz4_zstd && inputs.kernel_version == '6.12' && '-LZ4ZSTD' || '' }}
      CCACHE_DIR: ${{ github.workspace }}/.ccache-${{ inputs.kernel_version }}-${{ inputs.sub_level }}-${{ inputs.variant }}${{ inputs.use_susfs && '-susfs' || '' }}${{ inputs.use_droidspaces && '-ds' || '' }}${{ inputs.use_adios && inputs.kernel_version != '6.1' && '-adios' || '' }}${{ inputs.use_lz4_zstd && inputs.kernel_version == '6.12' && '-lz4-zstd' || '' }}
      CCACHE_COMPILERCHECK: content
      CCACHE_SLOPPINESS: file_macro,time_macros,include_file_mtime,include_file_ctime,pch_defines,modules

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 1

      - name: Resolve Reviewed Stack
        id: stack
        env:
          VARIANT: ${{ inputs.variant }}
          KERNEL: ${{ inputs.kernel_version }}
          SUBLEVEL: ${{ inputs.sub_level }}
          USE_SUSFS: ${{ inputs.use_susfs }}
          SUSFS_OVERRIDE: ${{ inputs.susfs_commit }}
          HOOK: ${{ inputs.hook_mode || 'manual' }}
          KOW_SPEC: ${{ inputs.kowsu_branch }}
          NEXT_SPEC: ${{ inputs.kernelsu_next_branch }}
          OFFICIAL_SPEC: ${{ inputs.kernelsu_official_branch }}
          RE_SPEC: ${{ inputs.resukisu_branch }}
          XX_SPEC: ${{ inputs.kernelsu_xx_branch }}
        run: |
          case "$VARIANT" in
            KowSU) SPEC="$KOW_SPEC" ;;
            KernelSU-Next) SPEC="$NEXT_SPEC" ;;
            KernelSU-Official) SPEC="$OFFICIAL_SPEC" ;;
            ReSukiSU) SPEC="$RE_SPEC" ;;
            KernelSU-XX) SPEC="$XX_SPEC" ;;
            *) echo "Unknown variant" >&2; exit 1 ;;
          esac
          python3 .github/scripts/resolve-stack.py \
            --variant "$VARIANT" --kernel "$KERNEL" --sublevel "$SUBLEVEL" \
            --susfs "$USE_SUSFS" --susfs-commit "$SUSFS_OVERRIDE" \
            --source "$SPEC" --hook "$HOOK"

      - name: Load Config
        id: cfg
        run: |
          config=$(jq -c '.["'${{ inputs.kernel_version }}'"]' .github/config/kernel_versions.json)
          echo "android_version=$(echo "$config" | jq -r '.android_version')" >> $GITHUB_OUTPUT
          sub_level="${{ inputs.sub_level }}"
          echo "sub_level=$sub_level" >> $GITHUB_OUTPUT
          revision=$(echo "$config" | jq -c '.revisions["'$sub_level'"]')
          echo "asb_date=$(echo "$revision" | jq -r '.asb_date')" >> $GITHUB_OUTPUT
          r_version=$(echo "$revision" | jq -r '.default_r')
          echo "r_version=$r_version" >> $GITHUB_OUTPUT
          echo "r_suffix=$([ "$r_version" != "none" ] && echo "_$r_version" || echo '')" >> $GITHUB_OUTPUT
          echo "clang_version=$(echo "$config" | jq -r '.default_clang')" >> $GITHUB_OUTPUT
          rust=$(echo "$config" | jq -r '.rust')
          echo "rust_enabled=$rust" >> $GITHUB_OUTPUT
          [ "$rust" = "true" ] && echo "rust_version=$(echo "$config" | jq -r '.default_rust')" >> $GITHUB_OUTPUT
          echo "mirror_tag=$(echo "$config" | jq -r '.mirror_tag')" >> $GITHUB_OUTPUT
          echo "anykernel_branch=$(echo "$config" | jq -r '.anykernel_branch')" >> $GITHUB_OUTPUT
          echo "kmi_version=$(echo "$config" | jq -r '.android_version')-${{ inputs.kernel_version }}" >> $GITHUB_OUTPUT

      - name: Free Disk Space & Install Dependencies
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL 2>/dev/null || true &
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            binutils python-is-python3 libssl-dev libelf-dev libdw-dev bison flex bc aria2 ccache

      - name: Restore Ccache
        if: inputs.use_ccache
        id: cache-restore
        uses: ./.github/actions/restore-cache-retry
        with:
          path: .ccache-${{ inputs.kernel_version }}-${{ inputs.sub_level }}-${{ inputs.variant }}${{ inputs.use_susfs && '-susfs' || '' }}${{ inputs.use_droidspaces && '-ds' || '' }}${{ inputs.use_adios && inputs.kernel_version != '6.1' && '-adios' || '' }}${{ inputs.use_lz4_zstd && inputs.kernel_version == '6.12' && '-lz4-zstd' || '' }}
          key: ${{ inputs.kernel_version }}-${{ inputs.sub_level }}-${{ steps.cfg.outputs.asb_date }}${{ steps.cfg.outputs.r_suffix }}-${{ inputs.variant }}${{ inputs.use_susfs && '-susfs' || '' }}${{ inputs.use_droidspaces && '-ds' || '' }}${{ inputs.use_adios && inputs.kernel_version != '6.1' && '-adios' || '' }}${{ inputs.use_lz4_zstd && inputs.kernel_version == '6.12' && '-lz4-zstd' || '' }}-${{ github.run_id }}
          restore-keys: ${{ inputs.kernel_version }}-${{ inputs.sub_level }}-${{ steps.cfg.outputs.asb_date }}${{ steps.cfg.outputs.r_suffix }}-${{ inputs.variant }}${{ inputs.use_susfs && '-susfs' || '' }}${{ inputs.use_droidspaces && '-ds' || '' }}${{ inputs.use_adios && inputs.kernel_version != '6.1' && '-adios' || '' }}${{ inputs.use_lz4_zstd && inputs.kernel_version == '6.12' && '-lz4-zstd' || '' }}-

      - name: Setup Ccache Wrappers
        if: inputs.use_ccache
        run: |
          for i in $(seq 1 60); do which ccache >/dev/null 2>&1 && break; sleep 1; done
          which ccache || { echo "ccache not found after waiting"; exit 1; }
          CCACHE_PATH="${{ github.workspace }}/.ccache-bin"
          mkdir -p "$CCACHE_PATH" && rm -f "$CCACHE_PATH"/*
          for prog in clang clang++ ld.lld llvm-ar llvm-nm llvm-objcopy llvm-objdump llvm-size llvm-readelf llvm-strip; do
            ln -sf "$(which ccache)" "$CCACHE_PATH/$prog"
          done
          echo "$CCACHE_PATH" >> $GITHUB_PATH
          ccache -M 10G \
            -o hash_dir=false \
            -o compiler_check=content \
            -o "sloppiness=file_macro,time_macros,include_file_mtime,include_file_ctime,pch_defines,modules" \
            -o compression=true \
            -o compression_level=1 \
            -o "cache_dir=${{ env.CCACHE_DIR }}" \
            -o "base_dir=${{ github.workspace }}" \
            -o file_clone=false \
            -o hard_link=false \
            -z

      - name: Download Toolchains & Kernel Source
        run: |
          mkdir -p kernel_workspace && cd kernel_workspace
          RELEASE_MIRROR="https://github.com/${{ github.repository }}/releases/download/${{ steps.cfg.outputs.mirror_tag }}"
          CLANG_TAR="clang-${{ steps.cfg.outputs.clang_version }}.tar.gz"
          case "${{ steps.cfg.outputs.asb_date }}" in
            lts)
              branch="${{ steps.cfg.outputs.android_version }}-${{ inputs.kernel_version }}-lts"
              KERNEL_TAR="${branch}.tar.gz"
              if [ "${{ inputs.source_mirror }}" = "google" ]; then
                KERNEL_BASE_URL="https://android.googlesource.com/kernel/common/+archive/refs/heads/${branch}"
              else
                KERNEL_BASE_URL="$RELEASE_MIRROR"
              fi ;;
            *)
              if [ "${{ steps.cfg.outputs.r_version }}" = "none" ]; then
                branch="${{ steps.cfg.outputs.android_version }}-${{ inputs.kernel_version }}-${{ steps.cfg.outputs.asb_date }}"
                KERNEL_TAR="${branch}.tar.gz"
                if [ "${{ inputs.source_mirror }}" = "google" ]; then
                  KERNEL_BASE_URL="https://android.googlesource.com/kernel/common/+archive/refs/heads/${branch}"
                else
                  KERNEL_BASE_URL="$RELEASE_MIRROR"
                fi
              else
                tag="${{ steps.cfg.outputs.android_version }}-${{ inputs.kernel_version }}-${{ steps.cfg.outputs.asb_date }}_${{ steps.cfg.outputs.r_version }}"
                KERNEL_TAR="${tag}.tar.gz"
                if [ "${{ inputs.source_mirror }}" = "google" ]; then
                  KERNEL_BASE_URL="https://android.googlesource.com/kernel/common/+archive/refs/tags/${tag}"
                else
                  KERNEL_BASE_URL="$RELEASE_MIRROR"
                fi
              fi ;;
          esac
          retry() { for i in $(seq 1 30); do "$@" && return 0; echo "Retry $i/30..."; sleep 30; done; return 1; }
          download_pids=()
          retry aria2c -s16 -x16 -k1M "$RELEASE_MIRROR/$CLANG_TAR" -o clang.tar.gz &
          download_pids+=("$!")
          retry aria2c -s16 -x16 -k1M "$RELEASE_MIRROR/build-tools.tar.gz" -o build-tools.tar.gz &
          download_pids+=("$!")
          if [ "${{ steps.cfg.outputs.rust_enabled }}" = "true" ]; then
            retry aria2c -s16 -x16 -k1M "$RELEASE_MIRROR/clang-tools.tar.gz" -o clang-tools.tar.gz &
            download_pids+=("$!")
            retry aria2c -s16 -x16 -k1M "$RELEASE_MIRROR/rust-${{ steps.cfg.outputs.rust_version }}.tar.gz" -o rust.tar.gz &
            download_pids+=("$!")
          fi
          if [ "${{ inputs.source_mirror }}" = "google" ]; then
            KERNEL_URL="https://android.googlesource.com/kernel/common/+archive/${{ steps.stack.outputs.kernel_commit }}.tar.gz"
          else
            KERNEL_URL="$KERNEL_BASE_URL/$KERNEL_TAR"
          fi
          retry aria2c -s16 -x16 -k1M "$KERNEL_URL" -o common.tar.gz &
          download_pids+=("$!")
          for pid in "${download_pids[@]}"; do wait "$pid"; done
          if [ "${{ inputs.source_mirror }}" != "google" ]; then
            echo "${{ steps.stack.outputs.kernel_archive_sha256 }}  common.tar.gz" | sha256sum --check --strict
          fi
          extract_pids=()
          for f in *.tar.gz; do
            (mkdir -p "${f%%.*}" && tar -xzf "$f" -C "${f%%.*}") &
            extract_pids+=("$!")
          done
          for pid in "${extract_pids[@]}"; do wait "$pid"; done
          actual_version=$(make -s -C common kernelversion)
          test "$actual_version" = "${{ inputs.kernel_version }}.${{ inputs.sub_level }}" || {
            echo "::error::Kernel archive version mismatch: $actual_version"
            exit 1
          }
          rm -f *.tar.gz
          rm -f common/android/abi_gki_protected_exports_* || true
          [[ "${{ inputs.kernel_version }}" == "6.6" ]] && echo "device_find_any_child" >> common/android/abi_gki_aarch64_xiaomi || true
          sed -i 's/ -dirty//g' common/scripts/setlocalversion || true
          sed -i ':a; /#ifdef USE_PKCS11_ENGINE/{N;N;s/#ifdef USE_PKCS11_ENGINE\n\(.*\)\n#endif/\1/; ta}' common/certs/extract-cert.c || true

      - name: Apply LZ4 1.10.0 and Zstd 1.5.7
        if: inputs.use_lz4_zstd && inputs.kernel_version == '6.12'
        working-directory: kernel_workspace/common
        run: |
          patch --batch --forward -p1 < "${{ github.workspace }}/.github/patches/zram/lz4-1.10.0.patch"
          patch --batch --forward -p1 < "${{ github.workspace }}/.github/patches/zram/zstd-1.5.7.patch"

      - name: Report LZ4/Zstd Compatibility
        if: inputs.use_lz4_zstd && inputs.kernel_version != '6.12'
        run: echo "::warning::LZ4 1.10.0 and Zstd 1.5.7 patches are supported only for kernel 6.12; this build will continue without them."

      - name: Apply CVE-2026-43499 Fix
        working-directory: kernel_workspace/common
        run: |
          if bash "${{ github.workspace }}/.github/scripts/check_cve.sh" > /dev/null 2>&1; then
            echo "CVE-2026-43499 fix already present."
            echo "CVE_FIXED=true" >> $GITHUB_ENV
          else
            patch -p1 --forward < "${{ github.workspace }}/.github/patches/${{ steps.cfg.outputs.kmi_version }}/cve-2026-43499-fix.patch" || true
            if bash "${{ github.workspace }}/.github/scripts/check_cve.sh" > /dev/null 2>&1; then
              echo "CVE-2026-43499 fix applied successfully."
              echo "CVE_FIXED=true" >> $GITHUB_ENV
            else
              echo "::warning::CVE-2026-43499 fix could not be verified after patch."
              echo "CVE_FIXED=false" >> $GITHUB_ENV
            fi
          fi

      - name: Setup KernelSU
        working-directory: kernel_workspace/common
        run: |
          mkdir -p commit_info
          FETCH_FULL_HISTORY=true bash "${{ github.workspace }}/.github/scripts/fetch-pinned.sh" \
            KernelSU "${{ steps.stack.outputs.ksu_commit }}" \
            "https://github.com/${{ steps.stack.outputs.ksu_repo }}.git"
          test "$(git -C KernelSU rev-parse 'HEAD^{tree}')" = "${{ steps.stack.outputs.ksu_tree }}"
          prefix="${{ steps.stack.outputs.ksu_prefix }}"
          printf '%s\n' "${{ steps.stack.outputs.ksu_base }}" > "commit_info/${prefix}_upstream_commit"
          printf '%s\n' "${{ steps.stack.outputs.ksu_tree }}" > "commit_info/${prefix}_source_tree"
          printf '%s\n' "${{ steps.stack.outputs.kernel_commit }}" > commit_info/kernel_commit
          git -C "${{ github.workspace }}" rev-parse HEAD > commit_info/workflow_commit
          cp "${{ github.workspace }}/.github/config/ksu-stacks.json" commit_info/ksu-stacks.json
          if [ "${{ inputs.use_susfs }}" = "true" ]; then
            printf '%s\n' "${{ steps.stack.outputs.ksu_repo }}" > "commit_info/${prefix}_susfs_patch_repo"
            printf '%s\n' "${{ steps.stack.outputs.ksu_branch }}" > "commit_info/${prefix}_susfs_patch_branch"
            printf '%s\n' "${{ steps.stack.outputs.ksu_commit }}" > "commit_info/${prefix}_susfs_patch_commit"
          fi
          bash "${{ github.workspace }}/.github/scripts/setup_ksu.sh"
          ln -sf ../KernelSU/kernel drivers/kernelsu
          grep -q "kernelsu" drivers/Makefile || echo 'obj-$(CONFIG_KSU) += kernelsu/' >> drivers/Makefile
          grep -q "drivers/kernelsu/Kconfig" drivers/Kconfig || sed -i '/endmenu/i source "drivers/kernelsu/Kconfig"' drivers/Kconfig

      - name: Setup SUSFS
        if: inputs.use_susfs
        working-directory: kernel_workspace/common
        env:
          SUSFS_DIR: ${{ github.workspace }}/susfs4ksu
          SUSFS_REPO: ${{ inputs.susfs_repo == 'GitHub' && 'https://github.com/midori01/susfs4ksu' || 'https://gitlab.com/simonpunk/susfs4ksu' }}
          SUSFS_MIRROR: ${{ inputs.susfs_repo == 'GitHub' && 'https://gitlab.com/simonpunk/susfs4ksu' || 'https://github.com/midori01/susfs4ksu' }}
          SUSFS_COMMIT: ${{ steps.stack.outputs.susfs_commit }}
          KMI_VERSION: ${{ steps.cfg.outputs.kmi_version }}
        run: |
          bash "${{ github.workspace }}/.github/scripts/fetch-pinned.sh" \
            "$SUSFS_DIR" "$SUSFS_COMMIT" "$SUSFS_REPO" "$SUSFS_MIRROR"
          cp "$SUSFS_DIR"/kernel_patches/fs/* fs/
          cp "$SUSFS_DIR"/kernel_patches/include/linux/* include/linux/
          for patch_file in "${{ steps.stack.outputs.patch }}" "${{ steps.stack.outputs.try_umount_patch }}"; do
            patch -p1 --dry-run --forward --batch --fuzz=0 --no-backup-if-mismatch < "$patch_file"
            patch -p1 --forward --batch --fuzz=0 --no-backup-if-mismatch < "$patch_file"
          done
          python3 "${{ github.workspace }}/.github/scripts/check-susfs-interface.py" .

      - name: Setup Manual Hooks
        if: >-
          (inputs.variant == 'KernelSU-XX' && inputs.hook_mode != 'hookless') ||
          (inputs.variant == 'ReSukiSU' && inputs.hook_mode == 'manual')
        working-directory: kernel_workspace/common
        run: |
          case "${{ inputs.variant }}" in
            KernelSU-XX)
              patch -p1 < "${{ github.workspace }}/.github/patches/${{ steps.cfg.outputs.kmi_version }}/scope-min-manual-hooks-v2.3.patch"
              ;;
            ReSukiSU)
              patch -p1 < "${{ github.workspace }}/.github/patches/${{ steps.cfg.outputs.kmi_version }}/resukisu-manual-hooks.patch"
              ;;
          esac

      - name: Setup Droidspaces
        if: inputs.use_droidspaces
        working-directory: kernel_workspace/common
        run: |
          patch -p1 < "${{ github.workspace }}/.github/patches/${{ steps.cfg.outputs.kmi_version }}/fix_sysvipc_kabi.patch"
          patch -p1 < "${{ github.workspace }}/.github/patches/${{ steps.cfg.outputs.kmi_version }}/ntsync_base_compat.patch"

      - name: Setup ReKernel
        if: inputs.use_rekernel
        working-directory: kernel_workspace/common
        run: |
          REKERNEL_DIR="${{ github.workspace }}/ReKernel"
          REKERNEL_X_DIR="${{ github.workspace }}/ReKernel_X"
          bash "${{ github.workspace }}/.github/scripts/fetch-pinned.sh" \
            "$REKERNEL_DIR" "${{ steps.stack.outputs.rekernel_commit }}" https://github.com/Sakion-Team/Re-Kernel.git
          bash "${{ github.workspace }}/.github/scripts/fetch-pinned.sh" \
            "$REKERNEL_X_DIR" "${{ steps.stack.outputs.rekernel_x_commit }}" https://github.com/myflavor/ReKernel-X.git
          cp -r "$REKERNEL_DIR"/LKM-Source drivers/rekernel
          sed -i 's/depends on MODULES/depends on KSU/' drivers/rekernel/Kconfig
          sed -i 's/obj-m := rekernel.o/obj-$(CONFIG_REKERNEL) += rekernel.o/' drivers/rekernel/Makefile
          echo 'obj-$(CONFIG_REKERNEL) += rekernel/' >> drivers/Makefile
          grep -q "drivers/rekernel/Kconfig" drivers/Kconfig || sed -i '/endmenu/i source "drivers/rekernel/Kconfig"' drivers/Kconfig
          cp -r "$REKERNEL_X_DIR"/LKM-Source drivers/rekernel_x
          sed -i 's/\bstart_rekernel\b/rkx_start/g; s/\bexit_rekernel\b/rkx_exit/g; s/\bsendMessage\b/rkx_sendMessage/g; s/\bnet_uid_add\b/rkx_net_uid_add/g; s/\bnet_uid_del\b/rkx_net_uid_del/g; s/\bregister_signal\b/rkx_register_signal/g; s/\bunregister_signal\b/rkx_unregister_signal/g; s/\bregister_netfilter\b/rkx_register_netfilter/g; s/\bunregister_netfilter\b/rkx_unregister_netfilter/g; s/\bregister_binder\b/rkx_register_binder/g; s/\bunregister_binder\b/rkx_unregister_binder/g' drivers/rekernel_x/*.{c,h}
          sed -i 's/obj-m += rekernel_x.o/obj-y += rekernel_x.o/' drivers/rekernel_x/Makefile
          echo 'obj-y += rekernel_x/' >> drivers/Makefile
          echo "RKX_VERSION=${{ steps.stack.outputs.rekernel_x_version }}" >> $GITHUB_ENV

      - name: Apply Unicode Bypass Fix
        working-directory: kernel_workspace/common
        run: |
          patch -p1 < "${{ github.workspace }}/.github/patches/unicode_bypass_fix_6.1+.patch"

      - name: Apply ADIOS I/O Scheduler
        if: inputs.use_adios && inputs.kernel_version != '6.1'
        working-directory: kernel_workspace/common
        run: |
          patch -p1 --forward --fuzz=3 < "${{ github.workspace }}/.github/patches/${{ steps.cfg.outputs.kmi_version }}/adios.patch"

      - name: Report ADIOS Compatibility
        if: inputs.use_adios && inputs.kernel_version == '6.1'
        run: echo "::warning::ADIOS is supported only for kernels 6.6 and 6.12; this build will continue without ADIOS."

      - name: Inject Kernel Configs
        uses: ./.github/actions/kernel-config
        with:
          defconfig_path: "kernel_workspace/common/arch/arm64/configs/gki_defconfig"
          build_config_path: "kernel_workspace/common/build.config.gki"
          kernel_version: ${{ inputs.kernel_version }}
          sub_level: ${{ steps.cfg.outputs.sub_level }}
          variant: ${{ inputs.variant }}
          enable_susfs: ${{ inputs.use_susfs }}
          localversion: ${{ inputs.kernel_localversion }}
          use_droidspaces: ${{ inputs.use_droidspaces }}
          use_rekernel: ${{ inputs.use_rekernel }}
          use_adios: ${{ inputs.use_adios }}
          use_lz4_zstd: ${{ inputs.use_lz4_zstd }}
          hook_mode: ${{ inputs.hook_mode }}
          ksu_debug: ${{ inputs.ksu_debug }}

      - name: Save Commit Info
        working-directory: kernel_workspace/common
        run: |
          COMMIT_DIR="$(pwd)/commit_info"
          mkdir -p "$COMMIT_DIR"
          echo "${CVE_FIXED:-false}" > "$COMMIT_DIR/cve_fixed_${{ inputs.kernel_version }}.${{ inputs.sub_level }}"
          cd KernelSU
          save_ksu_commit() {
            git rev-parse HEAD > "$COMMIT_DIR/${1}_ksu_commit"
            git log -1 --format=%cd --date=format:'%Y-%m-%d %H:%M %z' HEAD > "$COMMIT_DIR/${1}_ksu_date"
          }
          save_susfs_commit() {
            if [ "${{ inputs.use_susfs }}" = "true" ] && [ -d "${{ github.workspace }}/susfs4ksu" ]; then
              pushd "${{ github.workspace }}/susfs4ksu" > /dev/null
              git rev-parse HEAD > "$COMMIT_DIR/susfs_commit"
              git log -1 --format=%cd --date=format:'%Y-%m-%d %H:%M %z' HEAD > "$COMMIT_DIR/susfs_date"
              echo "${{ inputs.susfs_repo }}" > "$COMMIT_DIR/susfs_repo"
              popd > /dev/null
            fi
          }
          save_rekernel_commit() {
            if [ -d "${{ github.workspace }}/ReKernel" ]; then
              pushd "${{ github.workspace }}/ReKernel" > /dev/null
              git rev-parse HEAD > "$COMMIT_DIR/rekernel_commit"
              git log -1 --format=%cd --date=format:'%Y-%m-%d %H:%M %z' HEAD > "$COMMIT_DIR/rekernel_date"
              popd > /dev/null
            fi
          }
          save_rekernel_x_commit() {
            if [ -d "${{ github.workspace }}/ReKernel_X" ]; then
              pushd "${{ github.workspace }}/ReKernel_X" > /dev/null
              git rev-parse HEAD > "$COMMIT_DIR/rekernel_x_commit"
              git log -1 --format=%cd --date=format:'%Y-%m-%d %H:%M %z' HEAD > "$COMMIT_DIR/rekernel_x_date"
              popd > /dev/null
            fi
          }
          case "${{ inputs.variant }}" in
            KowSU)
              save_ksu_commit kowsu
              echo "${{ steps.stack.outputs.ksu_repo }}" > "$COMMIT_DIR/kowsu_ksu_repo"
              save_susfs_commit
              ;;
            KernelSU-Next)
              save_ksu_commit kernelsu_next
              echo "${{ steps.stack.outputs.ksu_repo }}" > "$COMMIT_DIR/kernelsu_next_ksu_repo"
              save_susfs_commit
              ;;
            KernelSU-Official)
              save_ksu_commit kernelsu_official
              echo "${{ steps.stack.outputs.ksu_repo }}" > "$COMMIT_DIR/kernelsu_official_ksu_repo"
              save_susfs_commit
              ;;
            ReSukiSU)
              save_ksu_commit resukisu
              echo "${{ steps.stack.outputs.ksu_repo }}" > "$COMMIT_DIR/resukisu_ksu_repo"
              save_susfs_commit
              ;;
            KernelSU-XX)
              save_ksu_commit kernelsu_xx
              echo "${{ steps.stack.outputs.ksu_repo }}" > "$COMMIT_DIR/kernelsu_xx_ksu_repo"
              save_susfs_commit
              ;;
          esac
          save_rekernel_commit
          save_rekernel_x_commit

      - name: Compile Kernel
        working-directory: kernel_workspace/common
        run: |
          WORK_DIR="${{ github.workspace }}/kernel_workspace"
          export PATH="${WORK_DIR}/clang/bin:${WORK_DIR}/build-tools/bin:${WORK_DIR}/clang-tools/bin:${WORK_DIR}/rust/bin:${PATH}"
          export LIBCLANG_PATH="${WORK_DIR}/clang/lib"
          if [ -d "${{ github.workspace }}/.ccache-bin" ]; then
            export PATH="${{ github.workspace }}/.ccache-bin:${PATH}"
          fi
          ROOT_REAL_PATH=$(dirname "$(readlink -f "$PWD")")
          export KBUILD_GENDWARFKSYMS_STABLE=1
          export KCFLAGS="-fdebug-prefix-map=$ROOT_REAL_PATH=. -fmacro-prefix-map=$ROOT_REAL_PATH=. -ffile-prefix-map=$ROOT_REAL_PATH=. -no-canonical-prefixes -pipe -fno-stack-protector -D__ANDROID_COMMON_KERNEL__ -Wno-unused-command-line-argument"
          export SOURCE_DATE_EPOCH="${{ inputs.build_epoch }}"
          export KBUILD_BUILD_TIMESTAMP="${{ inputs.build_timestamp }}"
          export KBUILD_BUILD_HOST="${{ inputs.build_host }}"
          export KBUILD_BUILD_USER="${{ inputs.build_user }}"
          export RKX_VERSION="${RKX_VERSION}"
          make -j$(nproc) LLVM=1 ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- O=out KCFLAGS+=-O2 KCFLAGS+=-Wno-error gki_defconfig
          cp out/.config commit_info/resolved.config
          python3 "${{ github.workspace }}/.github/scripts/check-stack-config.py" \
            out/.config "${{ inputs.variant }}" "${{ inputs.use_susfs }}" "${{ inputs.hook_mode || 'manual' }}"
          make -j$(nproc) LLVM=1 ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- O=out KCFLAGS+=-O2 KCFLAGS+=-Wno-error Image
          [ "${{ inputs.use_ccache }}" = "true" ] && ccache -s || true

      - name: Upload Commit Info
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: commit-info-${{ inputs.kernel_version }}-${{ inputs.sub_level }}-${{ inputs.variant }}${{ env.SUSFS_SUFFIX }}${{ env.DS_SUFFIX }}${{ env.ADIOS_SUFFIX }}${{ inputs.artifact_suffix }}
          path: kernel_workspace/common/commit_info/
          retention-days: 14

      - name: Verify KernelSU Official Toolkit symbols
        if: inputs.variant == 'KernelSU-Official'
        working-directory: kernel_workspace/common
        shell: bash
        run: |
          set -euo pipefail

          VMLINUX="out/vmlinux"
          SYSTEM_MAP="out/System.map"
          LLVM_NM="${{ github.workspace }}/kernel_workspace/clang/bin/llvm-nm"
          LLVM_OBJDUMP="${{ github.workspace }}/kernel_workspace/clang/bin/llvm-objdump"
          VMLINUX_SYMBOLS="${RUNNER_TEMP}/vmlinux.symbols"
          SYSTEM_MAP_SYMBOLS="${RUNNER_TEMP}/system-map.symbols"
          DIRECT_CALLS="${RUNNER_TEMP}/vmlinux-direct-calls"

          test -s "$VMLINUX" || { echo "::error::Missing $VMLINUX"; exit 1; }
          test -s "$SYSTEM_MAP" || { echo "::error::Missing $SYSTEM_MAP"; exit 1; }
          test -x "$LLVM_NM" || { echo "::error::Missing llvm-nm: $LLVM_NM"; exit 1; }
          test -x "$LLVM_OBJDUMP" || { echo "::error::Missing llvm-objdump: $LLVM_OBJDUMP"; exit 1; }

          grep -Fqx 'CONFIG_KSU=y' out/.config || {
            echo "::error::KernelSU is not built in"
            exit 1
          }
          grep -Fqx 'CONFIG_KPROBES=y' out/.config || {
            echo "::error::KernelSU Official Toolkit requires CONFIG_KPROBES=y"
            exit 1
          }

          required_symbols=(
            __arm64_sys_reboot
            ksu_sulog_emit_grant_root
            ksu_sulog_capture_sucompat
            ksu_toolkit_init
            ksu_toolkit_exit
          )
          required_call_targets=(
            ksu_sulog_emit_grant_root
            ksu_sulog_capture_sucompat
            ksu_toolkit_init
            ksu_toolkit_exit
          )

          if [ "${{ inputs.use_susfs }}" = "true" ]; then
            grep -Fqx 'CONFIG_KSU_SUSFS=y' out/.config || {
              echo "::error::SUSFS was requested but CONFIG_KSU_SUSFS is not enabled"
              exit 1
            }
            required_symbols+=(ksu_handle_faccessat ksu_handle_stat)
            required_call_targets+=(ksu_handle_faccessat ksu_handle_stat)
          else
            required_symbols+=(
              ksu_handle_faccessat_sucompat
              ksu_handle_stat_sucompat
              ksu_toolkit_is_ksud_exists
            )
            required_call_targets+=(
              ksu_handle_faccessat_sucompat
              ksu_handle_stat_sucompat
              ksu_toolkit_is_ksud_exists
            )
          fi

          "$LLVM_NM" --defined-only --format=posix "$VMLINUX" |
            awk 'NF { print $1 }' > "$VMLINUX_SYMBOLS"
          awk 'NF >= 3 { print $3 }' "$SYSTEM_MAP" > "$SYSTEM_MAP_SYMBOLS"
          "$LLVM_OBJDUMP" --disassemble --no-show-raw-insn "$VMLINUX" |
            awk '$0 ~ /[[:space:]](b|bl)[[:space:]]/ && $0 ~ /</ { print }' > "$DIRECT_CALLS"

          failed=0
          for symbol in "${required_symbols[@]}"; do
            if ! grep -Fqx "$symbol" "$VMLINUX_SYMBOLS"; then
              echo "::error::$symbol is missing from vmlinux"
              failed=1
            fi
            if ! grep -Fqx "$symbol" "$SYSTEM_MAP_SYMBOLS"; then
              echo "::error::$symbol is missing from System.map"
              failed=1
            fi
          done

          for symbol in "${required_call_targets[@]}"; do
            if ! grep -Fq "<$symbol>" "$DIRECT_CALLS"; then
              echo "::error::No direct call reaches $symbol; ThinLTO may have bypassed its probe"
              failed=1
            fi
          done

          [ "$failed" -eq 0 ] || exit 1
          printf 'Verified %d Toolkit symbols and %d direct call targets\n' \
            "${#required_symbols[@]}" "${#required_call_targets[@]}"

      - name: Export vmlinux for eBPF
        if: inputs.upload_vmlinux
        working-directory: kernel_workspace/common/out
        run: |
          [ -f "vmlinux" ] && zstd -T0 -10 vmlinux -o vmlinux.zst

      - name: Upload vmlinux Artifact
        if: inputs.upload_vmlinux
        uses: actions/upload-artifact@v7
        with:
          name: vmlinux-${{ inputs.kernel_version }}-${{ inputs.sub_level }}-${{ inputs.variant }}${{ env.SUSFS_SUFFIX }}${{ env.DS_SUFFIX }}${{ env.ADIOS_SUFFIX }}${{ env.ZRAM_SUFFIX }}${{ inputs.artifact_suffix }}
          path: kernel_workspace/common/out/vmlinux.zst
          retention-days: 1

      - name: Package Kernel
        working-directory: kernel_workspace
        run: |
          IMAGE="common/out/arch/arm64/boot/Image"
          [ -f "$IMAGE" ] || { echo "Binary Missing!"; exit 1; }
          ZIP_NAME="${{ inputs.variant }}_Kernel-${{ inputs.kernel_version }}.${{ steps.cfg.outputs.sub_level }}${{ steps.cfg.outputs.r_suffix }}${{ env.SUSFS_SUFFIX }}${{ env.DS_SUFFIX }}${{ env.ADIOS_SUFFIX }}${{ env.ZRAM_SUFFIX }}${{ inputs.artifact_suffix }}.zip"
          bash "${{ github.workspace }}/.github/scripts/fetch-pinned.sh" \
            anykernel "${{ steps.stack.outputs.anykernel_commit }}" https://github.com/midori01/AnyKernel3.git
          sed -i 's/^kernel\.string=.*/kernel.string=Autlin Kernel/' anykernel/anykernel.sh
          sed -i 's/^ui_print "Flashing .*"/ui_print "Flashing Autlin Kernel..."/' anykernel/anykernel.sh
          grep -Fqx 'kernel.string=Autlin Kernel' anykernel/anykernel.sh
          grep -Fqx 'ui_print "Flashing Autlin Kernel..."' anykernel/anykernel.sh
          cp "$IMAGE" anykernel/
          cd anykernel && rm -rf .git && zip -r9 "../$ZIP_NAME" *
          echo "ZIP_NAME=$ZIP_NAME" >> $GITHUB_ENV

      - name: Save Ccache
        if: inputs.use_ccache
        uses: actions/cache/save@v6
        with:
          path: .ccache-${{ inputs.kernel_version }}-${{ inputs.sub_level }}-${{ inputs.variant }}${{ inputs.use_susfs && '-susfs' || '' }}${{ inputs.use_droidspaces && '-ds' || '' }}${{ inputs.use_adios && inputs.kernel_version != '6.1' && '-adios' || '' }}${{ inputs.use_lz4_zstd && inputs.kernel_version == '6.12' && '-lz4-zstd' || '' }}
          key: ${{ inputs.kernel_version }}-${{ inputs.sub_level }}-${{ steps.cfg.outputs.asb_date }}${{ steps.cfg.outputs.r_suffix }}-${{ inputs.variant }}${{ inputs.use_susfs && '-susfs' || '' }}${{ inputs.use_droidspaces && '-ds' || '' }}${{ inputs.use_adios && inputs.kernel_version != '6.1' && '-adios' || '' }}${{ inputs.use_lz4_zstd && inputs.kernel_version == '6.12' && '-lz4-zstd' || '' }}-${{ github.run_id }}

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v7
        with:
          name: Kernel-${{ inputs.kernel_version }}-${{ inputs.sub_level }}-${{ inputs.variant }}${{ env.SUSFS_SUFFIX }}${{ env.DS_SUFFIX }}${{ env.ADIOS_SUFFIX }}${{ env.ZRAM_SUFFIX }}${{ steps.cfg.outputs.r_suffix }}${{ inputs.artifact_suffix }}
          path: kernel_workspace/${{ env.ZIP_NAME }}
